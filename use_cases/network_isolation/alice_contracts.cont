// tables
table update_by_alice:
        true ->                                        
                set_by_alice,
                (
                    val: < bs[8], { (0, 7) -> ([*], high) } >   // any value but high since it is set by alice
                )

